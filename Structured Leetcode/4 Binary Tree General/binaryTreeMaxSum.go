package main

func maxPathSum(root *TreeNode) int {
	//Depth first search to find optimal route A on left side, and optimal Route B on the right side
	//If the two routes plus the current node are bigger than just one side. That is the optimal route.
	//Return the combined routes if it's bigger than A and B, as well as return the largest of A or B with the current node added to it.
	//Once you're finished, return the largest route
}

func main() {
	maxPathSum()
}
